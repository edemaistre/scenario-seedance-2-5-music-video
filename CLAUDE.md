# Project Instructions

Read `README.md` first.

This repository creates an installable skill, not a music-video deliverable. Preserve the supplied master-audio contract, avoid private Scenario data, and never use em dash or en dash characters in repository prose.

Current state: complete and released. The installable skill, deterministic media tools, production references, 113-test suite, Scenario dry-run smoke, packaging, local installation, public repository, and v1.0.0 release are the delivery contract.

Run `python3 -B -m unittest discover -s tests -v` before every push. Keep the live Scenario schema authoritative, preserve user media outside this repository, and retain the supplied master as the final soundtrack.

## Resume

**Resume this work:** `claude --resume 019fdb6d-cde0-7ee3-858d-8f411dc18f50`
